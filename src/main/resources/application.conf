akka.javasdk.dev-mode.http-port = 9157

# The tracker is answered without a session, and the address it came from decides both
# the refusal list and the session identifier, so it has to reach the endpoint.
akka.http.server.remote-address-header = on
